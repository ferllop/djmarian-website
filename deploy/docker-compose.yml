version: "3.9"
services:
  apache:
    build: ./apache
    restart: ${APP_RESTART}
    ports:
      - ${APP_PORT}:80
    volumes:
      - ../build:/usr/local/apache2/htdocs

  eleventy:
    build: ./eleventy
    volumes:
      - ../src:/source
      - ../build:/build
